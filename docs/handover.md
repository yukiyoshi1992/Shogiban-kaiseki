# 作業引き継ぎ（2026-06-18〜19 セッション、2026-06-19続き×5、2026-06-20続き）

このファイルは直近セッションの作業ログ。次回セッションは
**`CLAUDE.md`（リポジトリ構造・コマンド・アーキテクチャ）と本ファイルを読めば続きから再開できる**ことを目的に作成。
プロジェクトの企画・要件・設計の全体像は`CLAUDE.md`を参照（旧`docs/引き継ぎ資料.md`は2026-06-19に`docs/@old/`へ移動・非メンテナンス化）。

## 2026-06-20：UAT case1（実機UAT初回）で見つかった課題①②を修正

ユーザーが実機UATを1ケース実施（`UAT/case1/`、写真11枚＋回答kif＋生成kif）。`UAT/case1/要望・課題.txt`に整理された2件の課題に対応した。実装詳細・設計判断はCLAUDE.mdの「UAT case1 (2026-06-20) fixes」節を参照。要点：

1. **課題①（対局終了が未分析画像を切り捨てる）**：全10手分の画像が`runtime/input`に同期済みの状態で対局終了ボタンを押したのに、生成KIFが7手分しかなかった。原因は、`watch_fragment`が5秒間隔のポーリングで未処理画像を処理する一方、対局終了/対局中止ボタンのハンドラが未処理分の有無を確認せず即座に`finish_game()`していたこと。新しい`"finishing"`フェーズを追加し、`request_finish()`が未処理画像の有無をチェック→残っていれば「分析対応中...お待ちください」を表示しつつ`watch_fragment`に処理を継続させ、すべて処理し終えてから自動的にKIFをリネームしてidleに戻るように修正（対局終了・対局中止の両方に同じ修正を適用、同じ不具合を抱えていたため）。
2. **課題②（準備完了後に対局準備をやり直せない）**：`phase=="ready"`のとき対局準備ボタンが無効化されていた。`prep_disabled`の許可状態に`"ready"`を追加するだけで対応（`run_preparation()`は元々内部で状態リセットしてから再判定する作りだったため、それ以上の変更は不要）。
3. **検証**：`streamlit.testing.v1.AppTest`で、実モデル・`classify_frame`をスタブ化したヘッドレススモークテストを実施（一回限りのスクリプトで、検証後は削除済み）。未処理画像が実際に解析されてから完了処理に進むこと、`phase=="ready"`で対局準備ボタンが有効なことを確認。`runtime/`はテスト後に汚染なしの状態に戻している。
4. **`.gitignore`に`UAT/`を追加**（ユーザー指示）。`UAT/case1/`の写真・KIFは今後もgit管理対象外（runtime/やtrain/dataと同じ扱い）。
5. **副産物（対応不要・確認済み）**：作業開始時に見つかった未コミットの差分（`docs/99 次回PJ/`配下xlsx2点の削除、`画面実行コマンド.txt`の実行コマンドをフルUNCパスに変更）はユーザーに確認済み。前者は次PJ用に別フォルダへ意図的に移動したもの（このリポジトリでは「削除」扱いで問題なし）、後者はUAT時にフルパス指定が必要だったための変更。`train/@old/models_backup_20260618/`が未追跡だった件はこのセッションのコミットで一緒に追跡対象にする。

### Discordとの連携（経過）
セッション中、ユーザーから（Discord Channels経由で）「確認待ちのタイミングで『PCを見てください』通知が来なかった」との指摘があった。前回セッションで要望として記録されたのみで未実装だった機能（`docs/handover.md`旧8番）であることを案内し、ユーザーは「優先度は低いので、UAT対応の手が空いたタイミングで進めて良い」と回答。**次回（または本セッション後半で余裕があれば）対応するタスクとして保留中**（`.claude/settings.local.json`へのNotificationフック等の追加が必要、未着手）。

### Discord「PCを見てください」通知ルールの実装（同セッション内、ユーザーがUAT対応中に並行実施）
ユーザーから「UATは自分で進めるので、Discord通知の件をお願いします」と確認を得て、課題①②対応と同じセッション内で実装した。詳細はCLAUDE.mdの「「PCを見てください」通知 (2026-06-20)」節を参照。要点：
- `.claude/hooks/discord_notify.py`を拡張し、`Stop`（既存、直近の応答テキスト転送）に加えて`Notification`イベント（`permission_prompt`/`idle_prompt`時に短い呼びかけを送る）にも対応。
- `.claude/settings.local.json`（gitignore対象）に両イベントのhook登録と`DISCORD_CHANNEL_ID`を設定（現在稼働中のDiscord Channels連携と同じチャンネルIDを再利用）。
- 手動でstdinに合成ペイロードを流し込んで動作確認、実際にDiscordへメッセージが届くことを`fetch_messages`で確認済み（Stop・Notification両方）。
- **副産物（発見・修正済み）**：`discord_notify.py`のデバッグログ出力先`_DEBUG_LOG`が、2026-06-19のフォルダ整理（`train/test_runs/`→`train/data/test_runs/`）に追従しておらず、古いパスに書き続けていた（気づかれず`train/test_runs/`が復活していた）。パスを修正し、復活していた古いフォルダは削除済み。
- **未確定・要観察**：`Notification`イベントのstdin JSONの正確なフィールド名はAnthropic公式ドキュメントにも明記がなく、複数の候補キーを試す防御的な実装にしてある。実際に本物の権限確認プロンプトが発生したタイミングで`train/data/test_runs/discord_hook_debug.log`を確認し、`raw_input`の内容が想定と一致しているか（特に通知種別・本文の抽出が空になっていないか）を次回以降に確認すること。

### 次回やること（優先順、本セッション時点で更新）

1. **【最優先】UATの継続** — `UAT/case1/`同様、追加のUATケースを実施し、新たな課題・要望が出れば随時対応する。
2. （要確認）Discord「PCを見てください」通知が実際の`Notification`イベント（本物の権限確認プロンプト等）でも正しく内容を抽出できているか、次に発生したタイミングでログを確認する。
3. （任意・余裕があれば）`classify_frame`の採用判定厳密化（CLAUDE.md「New held-out test data evaluation」節参照）。
4. （任意・低優先度、ユーザーより「リソースが余っていればやる程度」と確認済み）次PJ（Androidアプリ化、別フォルダへ移動済み）への引継ぎ情報整理。

## 2026-06-19 続きその5：フォルダ整理（UAT前段、`docs/chat.txt`の指示対応）

ユーザーから「次にUATを実施する前にフォルダ整理をしたい」との依頼（`docs/chat.txt`）。本セッションは**フォルダ整理のみ**を実施し、UATは翌日別セッションで実施する方針（コードの挙動は一切変更していない）。詳細・理由はCLAUDE.mdの「Folder reorganization (2026-06-19)」節を参照。要点：

1. **本番系コードを`src/`へ移動**：`train/src/app_streamlit.py` → `src/app_streamlit.py`、`train/src/run_realtime.py` → `src/run_realtime.py`。本番運用画面の実行pyが学習用ツール置き場（`train/`配下）にあるのは違和感があるとのユーザー指摘に対応。`run_realtime.py`は他スクリプトに依存しない自己完結ファイル、`app_streamlit.py`は同階層から`run_realtime.py`をimportする構造だったため、2ファイルを同じ新ディレクトリに移すだけで済み、ロジック変更は不要（`app_streamlit.py`内の`REPO_ROOT`相対階層・`MODEL_DIR`・docstringのパス例のみ修正）。
2. **`train/models/` → `src/models/`へ移動**：本番コードが実際に読み込む場所なので本番側に置くべき、というユーザー指摘に対応。今後の再学習はここを出力先とする（`train_model.py`等のdocstring例も更新済み）。
3. **`train/test_runs/` → `train/data/test_runs/`へ移動**：「`train/data/cells`と`train/test_runs`の階層がばらばらで気持ち悪い」というユーザー指摘に対応。両方とも本番非依存・gitignore対象の生成データという同じ性質なので、`train/data/`配下に揃えて深さを統一した。`.gitignore`の重複行（`train/test_runs`）も削除。
4. **意図的に対象外としたもの**：`train/src/`配下の学習・ラベリング・診断系スクリプト本体とキャリブレーションデータフォルダ、`train/data/学習用の撮影データ/`以下の生データのフォルダ名・構成。今回のご指摘の対象外と判断し変更していない。
5. **副産物**：移動作業中、前回セッションで起動されたままの`streamlit run train/src/app_streamlit.py`プロセス（旧パスを参照していた、サーバ起動から放置されていたもの）が`train/test_runs/`内のファイルをロックしていて移動をブロックしていた。ユーザーに確認のうえ停止してから移動を完了させた。次回セッション開始時、Streamlit画面を使う場合は新たに`streamlit run src/app_streamlit.py`で起動し直すこと。
6. **検証**：`py_compile`と`streamlit.testing.v1.AppTest.from_file("src/app_streamlit.py").run()`のヘッドレススモークテストで、移動後も例外なく動作することを確認済み。ブラウザでの実機確認はまだ（次回UATで実施）。
7. **副産物（解決済み）**：作業中、`.claude/hooks/`フォルダがディスク上から消えていて（`discord_notify.py`が存在しない）、`.claude/settings.local.json`も空（`{}`、Stopフック登録・`DISCORD_CHANNEL_ID`設定が消えた状態）になっているのを発見。`discord_notify.py`自体はgit追跡対象なので`git checkout --`で復元済み（コミットは不要、HEADと一致するだけ）。ただし`settings.local.json`は元々gitignore対象のため復元手段がなく、空のままにした。Discordでユーザー本人に確認したところ、**現在動作中の双方向「Channels」連携（`claude --channels plugin:discord@claude-plugins-official`、`docs/claude code/claude code 定常指示.txt`参照）に切り替えた際、旧来の一方向Stopフック方式を意図的に無効化したと思われる**とのことで、**再設定はせず現状維持**で確定。今後この一方向フックを復活させる場合は、Discord Developer Portalでチャンネルidとボットトークン（`docs/discord_token.txt`）を再設定する必要がある。
8. **Discordでの追加要望（未着手・要相談）**：ユーザーから「AskUserQuestion（選択肢確認）のようにPC画面側の対応が必要なタイミングで、Discordに『PCを見てください』と通知するルールを作れないか」という要望が出た。実現には`.claude/settings.local.json`へのhooks追加が必要（おそらく`Notification`フック等の調査が必要）。次回セッションでの検討事項として記録。

### 次回やること（優先順、本セッション終了時点で更新）

フォルダ整理が完了。次回の優先順位は変わらず：

1. **【最優先】StreamlitアプリのUAT（実機確認）** — `streamlit run src/app_streamlit.py`で起動し、`runtime/input`→`runtime/result`に対して4ボタン+メッセージwindow・手動キャリブレーションUIを実際にクリックして検証する。
2. （任意・余裕があれば）`classify_frame`の採用判定厳密化（CLAUDE.md「New held-out test data evaluation」節参照）。
3. （任意・低優先度、ユーザーより「リソースが余っていればやる程度」と確認済み）次PJ（Androidアプリ化、`docs/99 次回PJ/`に要件あり）への引継ぎ情報整理。
4. （要相談）Discord「PCを見てください」通知ルールの検討（上記8番）。

## 2026-06-19 続きその4：Streamlit画面実装開始＋Discordをwebhook→ボットに切替

### 1. Streamlit画面実装（骨格〜エラー中止まで実装完了、UATは次回）
`docs/01 要件定義/画面要件.xlsx`の確定仕様に基づき、`train/src/app_streamlit.py`を新規作成。設計判断・実装詳細はCLAUDE.mdの「Streamlit operator screen」節を参照。要点：
- `run_realtime.py`を`import`して判定ロジック（向き判定・赤丸キャリブレーション・classify_frame・KIF出力など）を再利用する方式を採用（ユーザー承認済み、ロジックの三重管理を避けるため）。
- 4ボタン（対局準備/対局開始/対局終了/対局中止）+メッセージwindowを実装。
- 対局準備：自動3チェック（向き判定→赤丸キャリブレーション→初期配置照合）→失敗時は`streamlit-image-coordinates`による向き選択+四隅クリックの手動キャリブレーションUIにフォールバック。
- 対局開始：`st.fragment(run_every=5)`で`runtime/input`を定期監視、新規画像をclassify_frameで解析しKIF逐次更新。KIFファイル名は仕様の`yyyyMMdd_hh:mm`がWindowsで使えないため`yyyyMMdd_hh-mm`（ハイフン）に変更（ユーザー承認済み、唯一の仕様からの逸脱点）。
- 画面要件No.6（認識エラー時の自動中止）も実装済み：classify_frameが"error"を返した時点で即座に【対局エラー中止】にリネームして対局を終了する。
- **検証状況**：ブラウザでのUI実機テストはまだ未実施（次回ユーザーと一緒にUATを行う予定）。`streamlit.testing.v1.AppTest`を使ったヘッドレスのコード経路スモークテストは実施済み：対局準備（実際にruntime/input内の既存画像で自動チェック失敗→manual_calibへの遷移を確認）、手動キャリブレーションの向き選択→四隅クリック画面遷移、対局開始（KIF即時作成・既存ファイルのベースライン取得を確認）、監視中の1サイクル（認識→classify_frame→自動中止→ファイルリネームを確認）、対局終了/対局中止ボタン、いずれも例外なし。スモークテストで使った一時ファイルは`runtime/`から削除済み（`runtime/`は本番専用ルールを維持）。
- **未実施**：実際のブラウザでの動作確認（UAT）。優先度2,3の任意項目（classify_frame厳密化、向き判定への駒位置併用）。
- ユーザーが`python -m streamlit run train/src/app_streamlit.py --server.headless true`で実際に起動し、エラーなく立ち上がることを確認済み（コマンドは`画面実行コマンド.txt`としてリポジトリルートに保存・git管理対象）。**これは起動確認のみで、4ボタンを実際にクリックして一連の流れを確認するUATは次回（明日以降）実施予定。**

### 3. Discord「Channels」機能の調査（次回検討事項）
ユーザーから「Discordとの連携（双方向）を進めたい」との要望があり、claude-code-guideエージェントに調査を依頼した。結果：
- Claude Code公式の**Channels機能**（研究プレビュー）がDiscord/Telegram/iMessageに対応しており、Discord側から送ったメッセージをClaude Codeが受け取って応答する**双方向連携**が可能。
- 設定はすべてユーザー自身の操作が必要（Claude Codeのエージェントからは実行不可）：
  1. Discord Developer PortalでBot作成、Message Content Intent有効化
  2. Claude Code内で `/plugin install discord@claude-plugins-official` → `/reload-plugins`
  3. `/discord:configure <ボットトークン>`
  4. `claude --channels plugin:discord@claude-plugins-official` で再起動
  5. Discordでボットへ自分からDMを送り、返ってきたペアリングコードで `/discord:access pair <コード>`
  6. アクセス制限のため `/discord:access policy allowlist` を設定推奨（誰でもメッセージを送れる状態は危険）
- Team/Enterprise契約の場合は管理者がclaude.ai側でChannelsを有効化する必要がある場合がある。
- **既存の一方向Stopフック（webhook→ボット切替済み、`.claude/hooks/discord_notify.py`）とは別の仕組み**。Channelsが使えるようになったら、通知が重複する可能性があるので一方向フックの扱い（残すか無効化するか）を次回検討すること。
- 次回セッション開始時、ユーザーがこの設定を進めているかどうか確認すること。

### 2. Discord通知をwebhookからボットへ切替
ユーザーの方針転換により、Discord通知の送信方式をwebhookから実際のDiscordボットに変更。
- ボットトークンは`docs/discord_token.txt`に保存（gitignore対象、`.gitignore`に追記済み）。
- `.claude/hooks/discord_notify.py`を改修：`DISCORD_WEBHOOK_URL`へのPOSTから、`https://discord.com/api/v10/channels/{channel_id}/messages`への`Authorization: Bot <token>`付きPOSTに変更。トークンは環境変数ではなく`docs/discord_token.txt`から直接読む実装（トークン更新時に`settings.local.json`を触らなくて済むため）。
- `.claude/settings.local.json`の`env`を`DISCORD_WEBHOOK_URL`→`DISCORD_CHANNEL_ID`に変更（チャンネルIDは旧webhook URLをGETして`channel_id`フィールドから取得）。
- 手動テストで200成功を確認済み。
- **副産物**：このセッション中盤で「Discordに通知が来ない」という報告があり、調査の結果、(a) フックコマンドのパスを絶対パスに変更（相対パスでは自動発火しなかった可能性）、(b) `discord_notify.py`に呼び出しごとのデバッグログ（`train/test_runs/discord_hook_debug.log`、gitignore対象）を追加、という2点を対応済み。今後通知が来ない場合はまずこのログを確認すること。また、Stopフックは「Claudeが応答を完全に終えてユーザーに制御を返すタイミング」でのみ発火する（1ターン内の個々のメッセージ単位では発火しない）ため、長い作業中のターンでは通知が遅れて見える点もユーザーに説明済み。
- **重要**：この連携は一方向（Claude→Discord）のみ。Discord側からの返信をClaude Codeが読み取る機能はない（ユーザーから質問があり明確化済み）。

## 2026-06-19 続きその3：run_realtime.py修正の実機確認／フォルダ整理／新テストデータ評価／Discord連携

### 1. run_realtime.pyの手動キャリブレーションフローを実機で確認（完了）
前回（続きその2）で実装した`manual_calibration_flow()`を実際にユーザーと一緒に起動して確認。途中、立ち上がった画像が「先手右・後手左」に見えるという指摘があり、**Claude側で証明しようとせず、ユーザーに目視確認を依頼する**運用を徹底（CLAUDE.mdの001-raw節の教訓通り）。最終的に「合っている」と確認を得てからキャリブレーションを完了。手動キャリブレーションのGUIセッションは誤操作（`q`での取り消し）で2回やり直しになったが、最終的に保存成功（`実施しました`とユーザー確認済み）。

### 2. 新テストデータ評価
`train/data/学習用の撮影データ/テストデータ（対局画像＋対局KIF）/`の001・002を、ユーザー目視確認済みの向き・手動キャリブレーションで評価：
- **002（青背景・画像回転必要）：114/114フレーム完全一致、`detect_move.py`で113/113手正解、エラー0。** 完全に成功。
- **001（赤背景・画像回転不要）：117/118フレームの個別認識は完全一致**（`IMG_20260618_133745.jpg`のみ、直前の状態と一致＝重複/非指し手写真とユーザーが確認）。ただし`detect_move.py`の実際のKIF復元は**78/117手成功、move80以降エラー連鎖**という結果になった。原因を特定したところ、`classify_frame`の採用判定が「diff<=2なら採用」という閾値ベースであるため、重複写真の次の写真（実は2手分の差分を含む）が**1手だけの不完全な説明**で誤って採用され、そのまま1手分のズレが蓄積→80手目付近で許容範囲を超えてエラー連鎖、という設計上の弱点が判明。詳細はCLAUDE.mdの「New held-out test data evaluation」節を参照。**この弱点はまだ修正していない**（次回の検討事項）。ユーザーの判断で「データ自体の特殊事情（重複撮影）」と割り切り、001の評価は「既知の注意事項付きでOK」として確定。

### 3. フォルダ整理（`@old/`運用ルールの導入）
ユーザー指示「暫定的に使ったものでもう使わないものは各階層に`@old`フォルダを作ってそこに入れる」に従い、以下を整理（削除はせず移動のみ）：
- `docs/引き継ぎ資料.md` → `docs/@old/`（`docs/handover.md`に完全移行済みのため）
- `docs/001生データ認識誤り分析.md`（180度回転誤りと判明する前の誤った分析）→ `docs/@old/`
- `train/models_backup_20260618/` → `train/@old/`（3回目再学習が検証済みのため不要に）
- `train/data/確認用/`（完了済みの目視確認用グリッド画像）→ `train/data/@old/`
- `train/src/check_one.py`, `extract_cells.py`, `extract_game.py`, `extract_endgame.py`（旧引き継ぎ資料に「重複・旧データ専用」と明記されていたスクリプト）と、それらが使っていた`data{0-8}*_calib.json`の散在ファイル33個、`calibrate.py`のデフォルト出力名のまま放置されていた`calibration.json` → `train/src/@old/`
詳細はCLAUDE.mdの「`@old/`convention」節を参照。

### 4. Discord通知連携の追加
ユーザーから「Claudeからの主要な回答をDiscordにも送ってほしい」と依頼があり、Webhook URL経由でStopフックを設定した：
- `.claude/hooks/discord_notify.py`（Git管理、秘密情報なし）：Stopフックのstdin JSONから`transcript_path`を読み、セッションのJSONLから直近のassistantテキストを抽出し、`DISCORD_WEBHOOK_URL`環境変数のWebhookへPOSTする。
- `.claude/settings.local.json`（`.gitignore`に追加・Git管理外）：実際のWebhook URLと、Stopフックの登録（`python3 .claude/hooks/discord_notify.py`）を保持。
- 動作確認済み（curl経由のテスト送信もパイプテストも成功、ユーザーが実際にDiscordでメッセージを確認）。**注意**：urllib.request単体だとDiscordが403を返すため、curlをsubprocess経由で呼ぶ実装にしている。
詳細はCLAUDE.mdの「Discord notification hook」節を参照。新しい環境で再現する場合は`.claude/settings.local.json`を手動で作り直す必要がある（Git管理外のため）。

## 2026-06-19 続きその2：001生データの誤読を全件洗い出し（進行中）

前回セッション終了時点の最優先タスク「`001 ※画像が90度回転して入ってきている場合のパターン`を100%にする」に着手。

**ユーザーからの方針確認**：3回目の再学習には既に001のデータ（001_180fix経由、3枚分）も含まれているため、
「また再学習する」のは意味がないという判断。諦めて次のタスクに進むのでもなく、**まず原因を追求する**方針。
誤っている箇所を全部洗い出して一覧化し、それを見てから一緒に対応を考える。目視確認は前回同様ユーザー側が行う
（Claudeの画像認識は信頼性が低いため、CLAUDE.mdの該当節の通り）。

### 新規ツール: `train/src/audit_recognition.py`
`detect_move.py`の`classify_frame`は失敗すると盤面を凍結するため、その後の全フレームの差分が
「本当の誤読」か「凍結状態を引き継いだだけの見かけ上の差分」か区別できない（カスケード）。
これを避けるため、`label_from_kif.py`の`load_kif_boards()`を再利用してKIFを再生し、
**各フレームをその時点の正解局面と直接1:1で比較**する（カスケードなし、全フレーム独立判定）診断ツールを新規作成した。
使い方はCLAUDE.mdの「Commands」節と「audit_recognition.py」節を参照。

### 実施結果（詳細は`docs/@old/001生データ認識誤り分析.md`）
- 001生データ79枚全てを直接比較。完全一致は3/79フレームのみ、延べ2156/6399マスが誤読。
- **誤読は特定マスに集中**（一部のマスは79枚中50〜70枚で恒常的に誤読）。ランダムな単発ミスではない。
- **確信度は無意味**：正解時・誤読時とも平均確信度1.0000、誤読の100%が確信度0.99以上。低確信度フィルタでは検出不可能。
- **キャリブレーション起因ではないことを確定**：同じ79枚の写真（同一ファイル名）を、全く独立した別経路
  （`001_180fix/`フォルダ＋既存`対局v2_calib/001_calib.json`＋回転なし指定）で再検証した結果、
  フレーム単位・マス単位まで**完全に同一の結果**になった。座標変換・透視変換のバグは排除でき、
  純粋にモデルの認識精度の問題と確定。
- 誤読頻度が高いマスは、盤の中心に対して点対称な位置(row,col)/(8-row,8-col)で色が反転したペアとして現れ、
  かつ駒種の取り違え（金/玉、銀/金、飛/空白など）も対応する。両者の陣営で独立に同種の駒の見分けに失敗している
  ことを示唆し、座標バグではなくこの部屋の照明下でのモデルの弱点と考えられる（推測。目視確認待ち）。
- 目視確認用に`train/test_runs/@old/001raw_mismatches_full.csv`（全2156件）、
  `train/test_runs/@old/001raw_review_crops/`（上位12マスの代表フレーム切り出し画像、マス境界・ファイル名に
  row/col/正解/予測/確信度を明記）、盤面全体にrow,col番号を焼き込んだ参照画像3枚を用意した
  （後に180度の向き誤りと判明・解決したため、現在は`train/test_runs/@old/`に移動済み）。

### ユーザーによる目視確認結果（重要・前の結論を訂正）
`001raw_review_crops/`をユーザーが目視確認した結果（`docs/chat.txt`参照）：
- `fullboard_*`（盤全体）は緑グリッドと盤のマス目がほぼ重なっており問題なし。
- しかし`row0,col3`/`col5`/`col6`、`row2,col0`の切り出し画像では、**赤枠（マス境界）と実際の
  盤のマス目がズレていて、駒が枠から見切れている**ことが判明。点対称の反対側
  （`row8,col2`/`col3`/`col5`、`row6,col8`）は問題なし、とのこと。

→ 「2つの独立したキャリブレーション経路が完全一致したのでキャリブレーションは原因ではない」
という前の結論は**誤り（言い過ぎ）だった**。2経路とも同じ物理的な赤丸マーカーから作った
透視変換なので、一致すること自体は「コーナー座標が大きく違う値で打たれていない」ことしか
証明しておらず、「その4点ホモグラフィ自体が幾何学的に正確」であることは何も証明していなかった。

調査の結果：`auto_calibrate.py`の赤丸検出を001生データの**全79フレーム**に対して厳密な閾値で
実行しても、**1枚も4個の赤丸を検出できない**ことが判明。特に右上隅のマーカーは面積が他の隅の
約7%しかなく、回転後画像の右端（x≈2157/2160px）にほぼ張り付いている＝物理的にフレーム端で
切れている可能性が高い。重心座標自体はフレーム間で安定している（数px以内）ものの、不完全な
円弧からの重心推定はフルの円に比べ信頼性が低く、これが該当マスの歪みの原因と考えられる
（未確定。手動再キャリブレーションで改善するか検証が必要）。

### run_realtime.pyの本番フロー実装ミスを発見・修正（重要）
上記の議論の中で、ユーザーから「本番はエラーが出たらユーザがキャリブレーションするから同じ問題は
起きない」という認識が示されたため、実際の`run_realtime.py`のコードを確認した。
**CLAUDE.mdの記述（「nを選ぶと人間が手動4隅キャリブレーションする」）は実装と一致していなかった**：
- 赤丸が4個未満しか検出できない場合、`calibrate_from_image()`は`None`を返し、コードは
  「赤丸が見える状態で撮り直してください」と表示して次の新規画像を待つだけ（手動クリックへの
  導線が存在しない）。
- `[r]`（向き手動指定）は、赤丸検出に一旦成功した後（初期配置との不一致時）にしか出てこず、
  かつその実体も「別の回転方向を仮定して同じ自動検出をやり直す」だけで、実際の4隅クリックは
  どこにも実装されていなかった。
- つまり001のように赤丸が構造的にどの写真でも検出できない部屋では、本番でも「撮り直してください」
  ループから抜け出せず、本当の手動救済に到達できない欠陥があった。

ユーザーから正式な基本プロセス（`docs/chat.txt`に記載、`画面要件.xlsx`のプロセスシートにも
追記予定）を提示され、その通りに`run_realtime.py`を修正した：
1. 向き判定(青三角)→赤丸キャリブレーション→初期配置照合、の3つを自動で順に試す。
2. **いずれか1つでもエラーになったら即座に**人間が向き指定+四隅クリックを行う（新設
   `manual_calibration_flow()`、`calibrate.py`と同じマウスクリックUIを本ファイルに内包）。
   「撮り直してください」という自動リトライループは一切行わない。
3. 人間が決めた結果は無条件に採用し、再検証せずそのまま準備OKへ進む。
4. 準備OK表示後、Enter押下（画面UI実装時は「対局開始」ボタンに相当）で対局開始。

**未実装として残した項目**（範囲が大きい・画面UI実装と密接なため）：
- 画面要件.xlsx No.6「認識エラー時の自動中止（ファイル名先頭を【対局エラー中止】に変更）」は
  `run_realtime.py`にまだ実装していない。現状は`[認識ミス]`と表示して待つだけ。KIFファイル名の
  状態遷移はStreamlit実装側で持つのが自然なため、そちらでの対応を想定し記録のみ。
- 向き判定に駒位置も使う（青三角だけでなく）という拡張は、ユーザーが「可能であれば」と
  ニースツーハブ的に言及したのみで未実装（`detect_move.py`の`detect_orientation_from_initial()`
  に同種ロジックがあるので移植は可能）。

ユーザーからの追加方針：**テストであってもキャリブレーションはユーザー自身に確認してもらう**
（赤丸キャリブレーションの精度不足によるエラー・後戻りが何度も発生しているため）。次回以降、
新テストデータのキャリブレーションも、Claudeが独断で進めずユーザーに確認を依頼すること。

### 解決（2026-06-19）：001生データの真因は「向きが180度間違っていた」だけだった

手動キャリブレーション用にユーザーが画像を開いたところ、「先手が右・後手が左になっている、
180度間違っていないか」とすぐに指摘があった。これは前のセッションでも指摘されていた懸念。
Claude側でピクセル差分等で「証明」しようとする前に、180度補正した画像を作ってユーザーに
目視確認してもらう方式に切り替えた結果、「合っている」と確認。

その画像で手動4隅クリックキャリブレーション（`train/test_runs/001raw_calib_v2.json`）を実施し、
`--force-direction down`（青三角の自動判定は`up`だったが、実際は`up`からさらに180度の`down`が
正しかった）で`audit_recognition.py`を再実行した結果：

**79フレーム全て完全一致（誤読0/6399マス）。`detect_move.py`でも78手中78手正解、
生成KIFが正解KIFと完全一致（exact match: True）。**

つまり、ここまでの長い調査（モデルがkin/ouを混同する、確信度が使えない、赤丸マーカーが
劣化している、等の分析）は**全て誤りで、実体はモデルもキャリブレーション方式も最初から
問題なかった**。180度ズレた画像を分析すると、将棋盤の点対称構造により「点対称の位置で色が
反転したペア」という誤読パターンがちょうど生まれる（盤面は元から先手後手で点対称＋色反転の
構造を持つため）。これが「モデルの系統的な弱点」のように見えてしまっていた。

**教訓（重要・今後も同じ間違いを繰り返さないこと）**：
「2つの独立したキャリブレーション経路が完全一致したのでキャリブレーションは正しい」という
Claude側の検証は、向きが間違っているケースを全く検出できない（向きが間違っていても、2つの経路は
同じ間違った向きの画像を別々のクリック位置でキャリブレーションするだけなので、一致して当然）。
**向き判定が絡む場面では、Claudeの計算上の検証だけで完結させず、必ず実際に向きを揃えた画像を
ユーザーに見せて目視確認してもらうこと。** これは`run_realtime.py`の本番フロー修正
（向き判定/赤丸キャリブレーション/初期配置照合のいずれかでエラーなら即・人間が確認）にも
すでに反映されている。

`docs/@old/001生データ認識誤り分析.md`とCLAUDE.mdの「001-raw」節に、誤った調査過程の記録として
（反面教師として）残してある。

### 次回（または本セッション続き）でやること
1. 上記が完全に決着したので、優先順位1（新テストデータでの評価）・2（Streamlit実装）に進む。
   `audit_recognition.py`は新テストデータの評価にも転用できる（held-outデータでcalib作成後、
   同じ手順で精度測定可能）。
2. **新テストデータの向き確認も、必ずユーザーに目視確認を依頼すること**（今回と同じ「180度
   ズレ」のような問題を、Claude側の分析だけで見逃さないため）。キャリブレーションも含め、
   テストであってもユーザーに確認してもらう方針（ユーザー指示済み）。

## 2026-06-19 続き：旧テストデータでの再検証＋優先順位の調整

前回提示した次回作業リスト（新テストデータ評価／旧テストデータ参考再テスト／Streamlit実装／駒のずれ直し対応）について、ユーザーから次の方針確認があった：
- 「1（新テストデータ評価）の前に4（駒のずれ直し対応）はやらなくていいか？」という質問に対し、ユーザー自身の結論：**まず1,2をやってみて、精度が100%でなければ4を3より先にやる**（つまり結果次第で優先度が `3 < 4` に変わる）。
- 2（旧テストデータでの参考再テスト）について、対象フォルダを明確化：`学習データ（対局画像＋対局kif）3/001 ※画像が90度回転して入ってきている場合のパターン`（**生データ・回転未加工版**。`001 ※001の画像を正しい方向に90度回転加工済のデータ`や`001_180fix`とは別物のフォルダだが、KIFファイルは内容が同一＝同じ対局の別収録/別加工）と`002/`。

### 実施結果（detect_move.py本番相当パイプラインで再テスト）
3回目再学習済みモデル（検証精度99.94%）で、`detect_move.py`（青三角向き判定→回転→キャリブレーション→認識→差分→合法手照合→KIF出力）をフルパイプラインで実行：
- **002**：111/111手 正解、エラー0。生成KIFを`shogi.KIF.Parser`で再パースし正解KIFと完全一致を確認。
- **001（生データ・回転要）**：78手中55手正解（23エラー）。最初の方で3手（1,3,4手目）の単発グリッチ→5〜52手目はクリーン→53手目以降にエラーが集中（53-58,60-63,65-70,72-75）し、一度ズレると自己回復できず連鎖する（失敗フレームは盤状態を固定するため、次の比較が実質2手分の差分を説明しなければならなくなり、また失敗しやすい）。本番（`run_realtime.py`）であれば最初の失敗時点で「認識エラー→自動中止」になっていたはずの対局。

**結論：002（学習に使ったデータ）は100%だが、001（生データ・別の部屋/照明条件）は100%に届かなかった。**

### 53手目エラーの原因調査（ユーザー指摘により実施・確定）
ユーザーから「53手目の画像を見たら、駒のずれを手で直していそうだし、片方の画像だけ人の影で先手側が暗い」との指摘があり、実際に`IMG_20260616_150958.jpg`→`IMG_20260616_151012.jpg`を直接比較して確認：
- 150958.jpgはマウスを持つ手が写り込み、先手陣に影がかかっていて、銀・金・王付近の駒が傾いている。
- 151012.jpgは手が消え、影もなくなり、同じ駒が真っ直ぐ直されている。
**この間に実際の指し手は起きておらず、人が駒の傾きを直しただけ。** これがdetect_move.py/run_realtime.pyの差分検出ロジック（「差分→合法手に該当しなければ盤面を直前のまま固定して進む」という設計）と相性が悪く、1回の駒直しイベントが53手目以降の長いエラー連鎖（53-58,60-63,65-70,72-75）を引き起こしたと確定。**つまり今回の001の精度低下の主因は、モデル精度やキャリブレーションの問題ではなく、「対局中の駒のずれ直し」に対して認識パイプラインが無策であること（＝前回相談していた4番の課題そのもの）。**
（参考・未確定）51手目時点で既に正解KIFとの比較で81マス中約30マスが恒常的に不一致（信頼度1.0の自信ある誤読）であることも判明。53手目以前にも同様の駒直し/照明変化イベントが起きていた可能性があるが、これは未確認の推測。

### 4. 駒のずれ直し対応：classify_frame実装（完了）
ユーザー承認のもと、プランモードで設計→実装。詳細はCLAUDE.mdの「駒のずれ直し対応」節を参照。要点：
- `detect_move.py`/`run_realtime.py`の指し手判定を「直前の写真同士の比較」から「**boardの状態と今回の写真を比較し、合法手を総当たりで探す**」方式に変更（`classify_frame()`新設）。
- 1手で説明できなければ2手先まで探索（直前フレームが認識ミスで取りこぼされた場合の自己回復用）。
- 「写真がboardと完全一致(diff0==0)」の場合のみ「指し手なし」と判定する設計に**最終的に**した。**重要な教訓**：最初は「diff0が2以下なら指し手なしとみなす」という閾値方式で実装したが、これは安全ではないと判明（テストで実際に退行を確認）。「駒のずれ等のノイズ」と「モデルが見落とした本当の指し手」は差分の大きさだけでは区別できない（同じdiff=2になる）ため、小さい差分だからといって「指し手なし」と決めつけてはいけない。曖昧なら必ずエラー（保留）にする。
- 旧ロジックの`detect_changes`/`infer_move`/`find_legal_move`(`find_legal`)、`run_realtime.py`の`prev_labels`等は使われなくなったため削除。
- **002で退行なし確認**（111/111、正解KIFと完全一致）。

### 5. 001生データの再テスト結果（駒のずれ直し対応後）と、新たな別問題の発見
新ロジックで001生データを再テストした結果、**3/78手のみ採用**（旧ロジックの55/78より少ない）。これは退行ではなく、**「曖昧な手を推測しない」設計により、旧ロジックが黒黙って誤った手を採用していたかもしれないケースを正しく「保留」にするようになったため**、件数として減っている。

調査の中で、駒のずれ直し（53手目）とは**別の問題**を発見：1,3,4手目のエラーの原因を深掘りしたところ、`IMG_20260616_150603.jpg`という1枚の画像で、モデルが「2筋（飛車の前の歩）の変化を見落とし、代わりに8筋（角の前の歩）の変化を誤って検出する」という**個別の認識ミス**が起きていた。
- 2つの独立したキャリブレーション経路（自分で作ったcalib／ユーザー提供の手動回転済みデータ＋既存`対局v2_calib/001_calib.json`）で**同じ結果**になり、座標・回転方向のバグではないことを確認。
- ユーザーが実画像を直接確認し、「`150527.jpg`→`150603.jpg`で先手の飛車前の歩が確実に1マス進んでいる」ことを確定（モデルはこれを見落としている）。
- ログのタイムスタンプで、この誤読が**今回のclassify_frame改修より前から**（3回目再学習直後の最初のテストから）存在していたことを確認済み＝今回の改修が原因ではない。
- この部屋・このタイミングの撮影データはcells/生成への貢献がほぼゼロ（3枚のみ）だったため、全体の検証精度99.94%とは別に、この部屋特有の認識精度の弱さが残っている可能性が高い。

**結論：001生データを100%にすることはできなかったが、原因は「駒のずれ直し対応の不足」（→対応済み）と「この部屋特有のモデル認識精度の限界」（→アルゴリズム改修では解決不可）の2つが混在していたことが分かった。** ユーザーの了承のもとこの結論で本セッションを終了する。

### 副産物：detect_move.pyのKIF出力バグを発見・修正
`moves_to_kif()`の移動元マス表記（`(27)`のような括弧内）が、全角の筋＋半角の段という非標準な混在表記（例: `(４1)`）になっていた。これ自体は見た目の問題に留まらず、**`shogi.KIF.Parser`で再パースすると最初に解釈できない手で例外を出さず黒黙って打ち切る**という実害があった（002の出力で111手中24手しかパースされなかった）。USI形式の升目名から筋・段を直接半角数字に変換するよう修正し、修正後は002の出力が正解KIFと完全一致することを確認済み。**この修正前に`detect_move.py`が生成したKIFファイルは、他ツールでの再パースを前提とするなら要再検証。**

### 副産物：auto_calibrate.pyの円形度フィルタが一部画像で4隅検出に失敗
001（回転後）の0手目画像で赤丸検出が2/4個しか通らなかった（円形度0.32-0.34がMIN_CIRCULARITY=0.45未満、半径自体は範囲内）。`auto_calibrate.py`は変更せず、フィルタ前の輪郭リストから手動で4隅座標を読み取って`compute_grid()`/`save_json()`で直接calibを作成する方法で回避（詳細はCLAUDE.mdの該当節）。頻発するなら閾値緩和を検討。

## 前回(2026-06-17)セッション後にサーバが落ちた件について

2026-06-17セッション終了後〜本セッション開始までの間にサーバ障害が発生し、以下が**未コミットのまま**残っていた。本セッション冒頭で復旧・検証済み：
- `doc/` → `docs/` フォルダの全面リネーム（ファイルシステム上は完了済み、gitには未反映だった）
- `train/src/label_from_kif.py` の `load_model`/`predict_board` 修正（後述、動作未検証のまま中断していた）

いずれも検証OKで `941a311` としてコミット・push済み。

## 今回やったこと

### 1. label_from_kif.py の実動作確認（完了）
前回中断していた修正（`meta["classes"]`→`meta["labels"]`、分類器ヘッドを`predict_cell.py`と同じ`Dropout→Linear(256)→ReLU→Dropout→Linear`構成に、入力リサイズを224pxに統一）を検証。
`shogi.KIF.Parser`（フォールバックではなく本処理）が実際に動作することを確認した。詳細はCLAUDE.mdの該当節を参照。

### 2. 対局データ3件のラベル付け実行（完了）
- 旧対局データ（学習データ（対局画像＋対局kif）2、72枚、`対局_calib/`）: **72/72枚採用、全て差分0**
- 002（学習データ（対局画像＋対局kif）3、112枚、`対局v2_calib/002_calib.json`）: **112/112枚採用、差分0〜1**
- 001（同上、79枚）: **当初0/79採用（差分35〜55、calibと画像の向きが噛み合っていなかった）**
  → 調査の末、"正しい方向に90度回転加工済"のはずの画像が実際にはcalibに対して180度ずれていたことが判明（move0で180度回転すると差分0になることを確認）。
  → 180度補正済みコピーを `学習データ（対局画像＋対局kif）3/001_180fix/` に作成し、それを使って再実行 → **3/79枚採用**（180度補正自体は正しいが、この部屋の照明条件下でのモデル認識精度が低いため大半はスキップ。これは既知の課題＝今回の再学習のそもそもの動機であり、label_from_kif.py側の不具合ではない）。

cells/ 合計: 17,982枚 → **33,129枚**（+15,147 = 187画像×81マス）

### 3. 目視確認中にpattern11の配置表バグを新規発見・修正（重要）
`check_labels.py`は対話的なcv2ウィンドウ表示でClaude Codeからは確認できないため、`make_grid()`を使ってラベルごとの確認用グリッド画像をファイル出力するワンオフスクリプトを書いて目視確認した。

`sente_ou`のグリッドに「駒が何も写っていない空白マス」が複数混在しているのを発見。調査の結果、`placement_patterns.json`の`pattern11`に**本来存在しないはずの余分な空白行**が1行多く定義されており、row3以降の駒ラベルが全て1行分ズレていた。pattern9/10a/10bは同じ手法で確認したが問題なし（pattern11のみの個別ミス）。

対応：
1. `placement_patterns.json`のpattern11から余分な空白行を削除
2. 汚染されていたpattern11由来のcellsファイル810枚を削除→修正後の表で再ラベル付け→正しく810枚再生成
3. 再度目視確認しOK

この教訓（行数ズレは警告が出ないので、calibで warp して row,col 番号を焼き込んで配置表と目視で照合する必要がある）はCLAUDE.mdに記載済み。

### 4. ユーザー指摘により目視確認をやり直し（重要・教訓）
最初の目視確認（上記3）は実は**pattern1〜8（盤画像＋駒配置表の最初のフォルダ）とdata1〜8、shift_baryu/kahigyoku/mix/narifullを完全に見落としていた**（pattern9〜11とshift_all/nariしか見ていなかった）。ユーザーから「サンプル数が少ない、全データソースが入っているか確認して」と指摘を受け、以下の対応をした：
- 全cells/ファイルをソースプレフィックス別（001/002/対局/data1〜8/pattern1〜11/shift_*の各種）に分類するロジックを書き、ラベルごとに**各ソースから層化抽出して最大200枚**を表示するグリッド画像を再生成（`make_grid`を再利用）
- 29ラベル全てを再度目視確認し、問題なし

**教訓**: 「目視確認した」と言うときは、サンプリング元が実際に全データソースをカバーしているか自分で検算すること。一部のソースだけ確認して「OK」と報告するのは不十分。

### 5. 駒台の赤丸について（ユーザー質問への回答）
駒台（盤外の付属品）の四隅にある赤丸は、`auto_calibrate.py`と`run_realtime.py`の両方で**意図的に半径でフィルタして除外する対象**であり、キャリブレーションには一切使われていない。物理的に消しても問題ない（むしろノイズが減って安定する）。

### 6. 3回目の再学習（完了）
`train_model.py --cells train/data/cells/ --out train/models/`（デフォルト20エポック）を実行。**所要時間 約11時間40分**（CPU学習、エポック10でベース層解凍してfine-tuning開始してからは1エポック45〜57分かかった）。

結果：
```
ベスト検証精度: 99.94%（エポック20、fine-tuning後）
エポック10まで（分類ヘッドのみ）: 92.6%→97.7%
エポック11以降（全体fine-tuning）: 99.8%→99.94%
```
新モデルは `train/models/best_model.pth` / `model_meta.json` に保存済み。
**旧モデル（再学習前）は `train/@old/models_backup_20260618/` にバックアップ済み**（新モデルが001/002で旧モデルより悪化した場合に戻せる）。

### 7. 画面要件（簡易UI）の検討（要件定義のみ、実装は次回）
ユーザーが `docs/01 要件定義/画面要件.xlsx` を作成。本番運用用の簡易画面（将来はAndroidネイティブアプリ化予定だが、まずは簡易版）の要件定義。
- 画面構成：4ボタン（対局準備／対局開始／対局終了／対局中止）＋メッセージwindow（テキスト表示領域）
- 各ボタンの挙動の疑問点をユーザーに確認し、回答を得て以下を確定（xlsxに反映済み）：
  - KIFファイル名は `【対局中】yyyyMMdd_hh:mm.kif`（日時=対局開始ボタン押下時刻）で開始
  - 対局終了→ファイル名先頭を`【対局完了】`に変更
  - 対局中止→`【対局中止】`に変更
  - **認識エラーで続行不能時は対局中止と同じ動作を自動実行し、ファイル名先頭は`【対局エラー中止】`**（run_realtime.pyの「人に確認を求める」仕様は古い想定で、誤ったKIFを生成するより諦める方針に変更。この行をxlsxのNo.6として追記済み）
- ツール選定：Streamlitで合意。Streamlitの実行モデル（ボタン押下で全体再実行）だと「対局開始後の継続的なフォルダ監視」は素直に書けない点を伝達済み→別スレッド/プロセス＋`session_state`ポーリングで対応する設計が必要、という認識を共有済み。手動キャリブレーション（四隅クリック）には`streamlit-image-coordinates`が必要、という点も共有済み。
- **環境準備のみ実施**: `pip install streamlit streamlit-image-coordinates` 完了（streamlit 1.58.0, streamlit-image-coordinates 0.4.0）。**アプリのコード自体はまだ書いていない**（ユーザーの意向で、学習完了後にセッションを切って次回新規セッションで実装する方針）。

### 8. 新しいテスト用データが追加された（次回セッションでテストすること、未着手）
ユーザーが学習に使っていない完全な held-out テストデータを追加：
```
\\YukiYoshiNAS\Shogiban-kaiseki-tool\train\data\学習用の撮影データ\テストデータ（対局画像＋対局KIF）\
  001 赤背景・画像回転不要パターン\   ... 118枚 + 解答.kif
  002 青背景・画像回転必要パターン\   ... 114枚 + 解答.kif
```
**重要**: これらは今回の学習データ生成（label_from_kif.py）に使っていない完全な未知データなので、精度評価として最も信頼できる。旧来テストに使っていた`学習データ（対局画像＋対局kif）3`の001/002は今回cells/生成にも使った（001は3枚、002は112枚全部）ため、それで再テストしても評価としては甘くなる可能性がある点に注意。
**calibはまだ存在しない**（新規データのため、`auto_calibrate.py`での自動キャリブor`calibrate.py`での手動キャリブが必要。001は「画像回転不要」、002は「画像回転必要」と明記されているのでオリエンテーション処理の参考に）。

## cells/ の現状（2026-06-19時点）
```
TOTAL: 33,129枚（pattern11修正後・全データソース目視確認済みの正しい状態）
```

## 次回やること（優先順、2026-06-19セッション続きその4終了時点で更新）

Discordのwebhook→ボット切替は完了。Streamlit画面（`train/src/app_streamlit.py`）は骨格〜対局終了/中止/エラー中止まで実装し、ヘッドレスのコード経路スモークテスト（`AppTest`）は通過済みだが、**ブラウザでの実機UAT・画面要件No.6以外の任意項目はまだ**。次回の優先順位：

1. **【最優先】StreamlitアプリのUAT（実機確認）** — ユーザーと一緒に実際に新しい対局を撮影しながら、`streamlit run train/src/app_streamlit.py`で本番想定フォルダ（`runtime/input`→`runtime/result`）に対して4ボタン+メッセージwindowの動作を確認する。手動キャリブレーションUI（向き選択+streamlit-image-coordinatesでの四隅クリック）も含めて実際にクリックして検証すること（コードレベルのスモークテストは済んでいるが、ブラウザでの見た目・操作感は未確認）。
2. **（任意・ユーザー承認済み、余裕があれば）`classify_frame`の採用判定を厳密化する** — 新テストデータ001で見つかった設計上の弱点（CLAUDE.md「New held-out test data evaluation」節）：`MOVE_DIFF_THRESHOLD<=2`での採用が、本来depth2で完全に説明できたはずのケースを不完全な1手で通してしまい、1手分のズレが後から大きなエラー連鎖を引き起こす。候補手を採用する条件を「diffが厳密に0になる手（必要なら深さを増やして探索）」に変更し、どの深さでも0に到達しなければ`"error"`とする方向の修正が考えられる（既存の「曖昧なら保留」方針と一致）。`run_realtime.py`/`detect_move.py`両方を修正し、002・新テスト001/002の3パターン全てで退行がないことを確認すること（`app_streamlit.py`は`run_realtime.py`をimportしているので、修正すれば自動的に反映される）。
3. **向き判定に駒位置も使う拡張**（ニースツーハブ、未着手）— `detect_move.py`の`detect_orientation_from_initial()`（rot180/swap候補の一致率判定）を`run_realtime.py`の対局準備フローに移植し、青三角判定のクロスチェックとして使う。

画面要件.xlsx No.6（認識エラー時の自動中止）は`app_streamlit.py`に実装済み（`watch_fragment()`が`classify_frame`の`"error"`を検知した時点で即座に`【対局エラー中止】`にリネーム）。

## 中断・再開について

- 本セッションのタスク管理（TaskCreate/TaskUpdate）はプロジェクトに保存されており、次回セッションでも`TaskList`で参照できる。
- 次回セッション開始時は「`CLAUDE.md`と`docs/handover.md`を読んで状況を整理して」と伝えれば、続きから再開できる。
- gitコマンドは本ネットワーク共有上で"dubious ownership"エラーが出る環境。CLAUDE.mdの方針通り`git config --global`はAgent側から実行しない（`git -c safe.directory='*' <command>`でその場限りの回避は可）。
- **重要（ユーザーからの指摘）**：盤面写真の目視確認はClaudeの画像認識精度が低く信頼できない。盤面の内容を確認したい場合は、モデルの出力（ラベル・row/col・確信度）を整理して提示し、実際の目視確認はユーザーに依頼すること。Claude自身が「画像を見て○○が動いている」と判断して報告するのは避ける（今回のセッションで実際に誤った解釈をして指摘を受けた）。
