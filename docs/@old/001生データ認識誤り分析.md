## 【2026-06-19 追記・最終結論】真因は画像の向き（180度）の誤りだった。以下は誤った分析過程の記録

**最終結論（解決済み）**: 本ドキュメントの当初の分析（モデルの認識精度の限界、キャリブレーションの精度問題）は
すべて誤りだった。真因は、青三角の自動向き判定が`up`（反時計90度回転）と判定していたが、
**実際に必要な回転は`down`（時計90度回転、＝up判定からさらに180度）だった**こと、それだけ。

ユーザーが手動キャリブレーション用に開いた画像を見て「先手が右・後手が左になっている、180度
間違っていないか」と指摘 → 180度補正した画像をユーザーが目視確認し「合っている」と確認 →
その画像で手動4隅クリックキャリブレーション（`train/test_runs/001raw_calib_v2.json`）を実施 →
`--force-direction down`で`audit_recognition.py`を再実行した結果、**79フレーム全て完全一致
（誤読0/6399マス）**。`detect_move.py`でも**78手中78手正解、生成KIFが正解KIFと完全一致**
（`exact match: True`）。

以下の「結論1」「結論2」「誤読頻度が高いマスの一覧」等は、**間違った向きで分析した結果**であり、
モデルやキャリブレーション方式そのものに欠陥はなかった。点対称の位置で色が反転したペアとして
誤読が現れていたのも、180度＋向き判定の誤りそのものが原因（180度ズレた画像は、真の盤面を
「180度回転＋先手後手入れ替え」した見え方に一致するため、ズレが大きいほど元の局面との非対称性が
そのまま誤読として現れる）。**この分析過程は「目視確認をユーザーに頼ることの重要性」「Claude側の
ピクセル比較だけでは方向の正しさを証明できないこと」の実例として、あえて以下に残す。**

詳細はCLAUDE.mdの「001-raw」節の追記、`docs/handover.md`も参照。

---

# 001生データ（学習データ（対局画像＋対局kif）3/001 ※画像が90度回転して入ってきている場合のパターン）認識誤り分析（誤った向きでの分析・参考用）

2026-06-19セッションで作成。`001生データを100%にする`タスクのための調査記録。
目視確認はユーザー側で行う方針（Claudeの画像認識は信頼性が低いため）なので、
このドキュメントは「どの手の・どの画像の・どのマスが・正解は何で・モデルは何と読んだか」を
機械的に洗い出した一覧と、そこから読み取れる傾向のみを記録する。

## 調査方法

`train/src/audit_recognition.py`（本セッションで新規作成）を使用。
従来の`detect_move.py`は「直前の差分から合法手を探す」方式のため、1回失敗すると盤面が
凍結し、以降の差分が雪だるま式に増える（カスケード）。これでは「本当にその手で誤読したのか」
「過去の凍結状態のせいで誤読扱いになっているだけか」を区別できない。

`audit_recognition.py`はこれを避けるため、各画像を**KIFを再生して得た「その時点の正解局面」と
直接比較する**（カスケードなし、画像[i] vs KIF局面[i]の1:1比較）。これにより、各フレーム・各マスが
本当に正しく認識できているかを独立に判定できる。

```bash
python train/src/audit_recognition.py \
  --kif    ".../001 ※画像が90度回転して入ってきている場合のパターン/001.kif" \
  --images ".../001 ※画像が90度回転して入ってきている場合のパターン/" \
  --calib  "train/test_runs/001raw_calib.json" \
  --model  "train/models/" \
  --out-pkl "train/test_runs/audit_001raw.pkl"
```

## 結論1: キャリブレーションのズレが原因ではない（確定）

同じ79枚の写真に対し、**2つの完全に独立したキャリブレーション経路**で比較した。

- 経路A: `001 ※画像が90度回転して入ってきている場合のパターン/`の生画像 + 青三角検出(up→反時計90度回転) + 本セッションで手動作成した`train/test_runs/001raw_calib.json`
- 経路B: 同じゲームの別収録写真`001_180fix/`（ファイル名は経路Aと同一79枚。別途180度補正済み）+ 回転なし + 既存の`train/src/対局v2_calib/001_calib.json`

結果は**完全に一致**（フレームごとの不一致マス数、マスごとの誤読パターンまで1件残らず同一）。
よってキャリブレーション/座標変換/回転方向のバグは排除でき、純粋にモデルの認識精度の問題と確定した。

## 結論2: 確信度(confidence)は誤読の手がかりにならない

| | 平均確信度 |
|---|---|
| 正解時 | 1.0000 (n=4243) |
| 誤読時 | 1.0000 (n=2156) |
| 誤読のうち確信度<0.9 | 0/2156 (0%) |
| 誤読のうち確信度>=0.99 | 2156/2156 (100%) |

モデルは誤読していても常に「確信度1.0」を返す。低確信度マスだけを機械的に検出して
人間に確認を求める、という運用は**この精度問題に対しては機能しない**。

## 全体傾向

- 全79フレーム中、完全一致（盤面81マス全て正解）は**3フレームのみ**。
- 延べ不一致マス数: 2156マス（全6399マス中）。
- 誤読は特定のマスに集中している（68マスで何らかの誤読が発生、しかしその中でも上位十数マスが
  圧倒的多数を占める）。ランダムな単発ミスではなく、**特定マスを継続的に間違え続けている**。

## 誤読頻度が高いマスの一覧（上位、79フレーム中）

| マス(row,col) | 不一致フレーム数 | 主な誤読パターン(正解→誤読) | 件数 |
|---|---|---|---|
| row2,col1 | 70/79 | sente_kin → sente_ou | 35 |
| row6,col7 | 70/79 | gote_ou → gote_kin | 35 |
| row2,col0 | 65/79 | empty → sente_gin | 56 |
| row6,col8 | 65/79 | gote_gin → empty | 56 |
| row0,col5 | 58/79 | empty → gote_fu | 58 |
| row0,col6 | 58/79 | gote_fu → empty | 58 |
| row8,col2 | 58/79 | empty → sente_fu | 58 |
| row8,col3 | 58/79 | sente_fu → empty | 58 |
| row3,col6 | 57/79 | gote_gin → empty | 25 |
| row5,col2 | 57/79 | empty → sente_gin | 25 |
| row0,col3 | 53/79 | empty → sente_fu | 53 |
| row8,col5 | 53/79 | gote_fu → empty | 53 |
| row2,col2 | 50/79 | sente_gin → sente_kin | 26 |
| row6,col6 | 50/79 | gote_kin → gote_gin | 26 |
| row1,col8 | 49/79 | empty → gote_hi | 42 |
| row7,col0 | 49/79 | sente_hi → empty | 42 |

**重要な特徴**: 上記の各行は、座標(row,col)と(8-row,8-col)が必ずペアで現れ、駒の色（先手/後手）も
入れ替わった形で対応している（例: row2,col1の「sente_kin→sente_ou」と、点対称の位置row6,col7の
「gote_ou→gote_kin」は、色を入れ替えると同じ「kin/ou取り違え」パターン）。これは盤の中心に対して
点対称な位置で、先手側・後手側それぞれが同種の駒の見分けに失敗している、ということを意味する
（金/玉、銀/金、飛/空、など）。座標変換のバグでこうなる可能性も検討したが、結論1の通り2つの
独立した経路で同一結果のため、座標変換ではなく**モデル自体がこの部屋の写り方でこれらの駒の
組み合わせを見分けられていない**と考えられる。

## row2,col1（sente_kin/sente_ou周辺）の時系列の例

```
idx  6 IMG_20260616_150641.jpg: true=empty      pred=sente_kin  conf=1.0000
idx  7 IMG_20260616_150645.jpg: true=empty      pred=sente_kin  conf=1.0000
...(idx6-11: 正解はまだ空きマスだが、モデルは金が既にあると誤読)
idx 12-14: 一致（モデルの読みと正解が一致）
idx 15 IMG_20260616_150718.jpg: true=sente_kin  pred=empty      conf=1.0000
...(idx15以降: 正解は金が来ているが、モデルは空きマスと誤読し続ける)
```
→ 金が実際にこのマスへ来る前後で、モデルの誤読の方向が逆転している。
  単なる「このマスはいつも誤読する」という固定バイアスではなく、駒の出現/不在どちらの状態でも
  このマスの認識自体が不安定、という可能性がある。

## 目視確認用に用意したファイル

- `train/test_runs/001raw_mismatches_full.csv` — 2156件の不一致を1行ずつ
  （frame_index, image_file, row, col, true_label, pred_label, confidence）で記録した完全版。
- `train/test_runs/001raw_review_crops/` — 上位12マスについて、各マスが誤読されている代表的な
  フレーム（最初・1/3地点・2/3地点・最後）を切り出した画像（マス境界を赤線で表示、周囲に少し
  マージンを付けて隣接マスの状況も見えるようにしてある）。ファイル名は
  `r{row}c{col}_idx{フレーム番号}_true-{正解}_pred-{モデルの予測}_conf{確信度}.jpg`。
- `train/test_runs/001raw_review_crops/fullboard_*_annotated.jpg` — 盤面全体に row,col 番号を
  焼き込んだ参照用画像（3フレーム分: 0手目、idx10付近、最終フレーム）。

## 次にユーザーに確認してほしいこと

1. `001raw_review_crops/`の切り出し画像を見て、上記の誤読が「本当にその駒が写っているように
   見えるか」「照明・ピント・反射などで紛らしい見え方になっていないか」を確認してほしい。
2. 特にrow2,col1 / row6,col7（金/玉の取り違え）のように、駒の種類自体が紛らわしいケースは
   この部屋の照明・カメラ位置に起因する可能性が高いと推測しているが、目視確認で裏付けたい。
3. もし「写真自体は明らかにこの駒だとわかる」のにモデルが外している場合は、学習データに
   この部屋の写真がほぼ含まれていない（cells/への寄与が3枚のみ）ことが原因と考えられ、
   再学習以外の対処は難しい可能性がある。逆に「写真自体が紛らわしい」場合は撮影条件の改善
   （照明・カメラアングル）も選択肢になる。

## 未解決・次回への持ち越し

- 上記の目視確認結果を受けて、対応方針（再学習する/しない、撮影条件の見直し、等）を決める。
- 新テストデータ（`学習用の撮影データ/テストデータ（対局画像＋対局KIF）`）でも同じ`audit_recognition.py`
  を使って同様の分析が可能（held-outデータでの精度評価にも使える汎用ツール）。
